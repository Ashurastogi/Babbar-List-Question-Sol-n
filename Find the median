public int find_fact(int[] v)
    {
        Arrays.sort(v);
        
        int n = v.length;
        
        if(n%2==0)
            return (v[n/2]+v[(n/2)-1])/2;
            
        return v[n/2];
    }
