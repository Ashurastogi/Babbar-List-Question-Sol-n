class Solution {
    int isPlaindrome(String s) {
        int n = s.length()-1;
        int i = 0;
        
        while(i<n){
            if(s.charAt(i)!=s.charAt(n))
                return 0;
                
            i++;
            n--;
        }
        
        return 1;
    }
};
